import { Box } from "@mui/material";
import { colors } from "./utils/colors";

function App() {
  return (
    <Box
      sx={{
        height: "100vh",
        width: "100%",
        backgroundColor: colors.primary,
      }}
    ></Box>
  );
}

export default App;
